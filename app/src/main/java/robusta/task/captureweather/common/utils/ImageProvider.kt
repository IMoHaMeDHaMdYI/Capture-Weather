package robusta.task.captureweather.common.utils

import androidx.core.content.FileProvider

class ImageProvider : FileProvider() {
    companion object {
        const val PACKAGE = ".common.utils.ImageProvider"
    }
}